const sketchGrid = document.querySelector('.sketch-box');
const sketchSquare = document.createElement('div');


const size = 2; // Remove! For testing gridSize.
let gridSize;

// for (let i = 0; i < size; i++;) {
//     sketchSquare;
// }

sketchGrid.appendChild(sketchSquare);
sketchGrid.appendChild(sketchSquare);